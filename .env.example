# GitHub classic Personal Access Token, no special scopes needed - used only
# to read your own public contributionsCollection breakdown (commits/PRs/
# reviews/issues) via GraphQL, which isn't available unauthenticated.
# Generate at https://github.com/settings/tokens
# Server-only: never prefix with NEXT_PUBLIC_.
GITHUB_TOKEN=
